const transliterateText = require("./transliterate");

try {
  const result = transliterateText("హైదరాబాద్", "auto", "gurmukhi");
  console.log(result);
} catch (error) {
  console.error("Error:", error.message);
}